name: Nightly Wheel Build

on:
  schedule:
    - cron: '0 20 * * *'  # daily 04:00 Beijing (UTC+8)
  workflow_dispatch:       # manual trigger

concurrency:
  group: nightly-${{ github.ref }}
  cancel-in-progress: true

env:
  CT: docker
  CONTAINER: mori_nightly_${{ github.run_id }}

jobs:
  # ── Stage 1: Build wheels ──────────────────────────────────────────────────
  build-wheel:
    name: wheel (py${{ matrix.python }})
    runs-on: [self-hosted, MI355X-AINIC-TW]
    env:
      IMAGE: rocm/mori:ci-py${{ matrix.python }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # py3.10 stays on 7.2.4: ROCm 7.14 ships no py3.10/ubuntu22.04 image.
          # Dockerfile.dev skips its apt ROCm 7.14 install when the base already
          # has a dev stack, so both lanes build from the same file.
          - python: "3.10"
            base_image: rocm/pytorch:rocm7.2.4_ubuntu22.04_py3.10_pytorch_release_2.8.0
          - python: "3.12"
            base_image: rocm/pytorch:rocm7.14_ubuntu24.04_py3.12_pytorch_release_2.12.0
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Clean stale dist
        run: |
          $CT run --rm -v ${{ runner.temp }}:/runner_temp ${{ matrix.base_image }} \
            rm -rf /runner_temp/dist || true

      - name: Build CI image
        run: $CT build --network=host --build-arg BASE_IMAGE=${{ matrix.base_image }} -t $IMAGE -f docker/Dockerfile.dev .

      - name: Build wheels
        run: |
          BASE_VER=$(git describe --tags --abbrev=0 | sed 's/^v//')
          NEXT_VER=$(echo "$BASE_VER" | awk -F. '{$NF=$NF+1; print}' OFS=.)
          DATE=$(date +%Y%m%d)
          SHA=$(git rev-parse --short HEAD)
          GH_VERSION="${NEXT_VER}.dev${DATE}+${SHA}"
          PYPI_VERSION="${NEXT_VER}.dev${DATE}"

          $CT run --rm --network=host \
            -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE -w $GITHUB_WORKSPACE \
            -v ${{ runner.temp }}/dist:/tmp/dist \
            $IMAGE bash -c "
              git config --global --add safe.directory '*'

              echo '=== Building CI test wheel (amd_mori ${GH_VERSION}) ==='
              SETUPTOOLS_SCM_PRETEND_VERSION=${GH_VERSION} \
                python3 -m pip wheel --no-deps -w /tmp/dist .

              echo '=== Building PyPI wheel (amd_mori_nightly ${PYPI_VERSION}) ==='
              sed -i 's/name = \"amd_mori\"/name = \"amd_mori_nightly\"/' pyproject.toml
              SETUPTOOLS_SCM_PRETEND_VERSION=${PYPI_VERSION} \
                python3 -m pip wheel --no-deps -w /tmp/dist .
              git checkout pyproject.toml
            "

          echo "=== produced ==="
          ls -lh ${{ runner.temp }}/dist/

      - name: Retag PyPI wheels (manylinux platform tag)
        run: |
          mkdir -p ${{ runner.temp }}/dist-pypi
          cat > ${{ runner.temp }}/retag.py << 'PYEOF'
          import zipfile, shutil, os, sys, tempfile
          src, dst, plat = sys.argv[1], sys.argv[2], sys.argv[3]
          shutil.copy2(src, dst)
          with zipfile.ZipFile(dst, 'r') as zin:
              wheel_file = [n for n in zin.namelist() if n.endswith('/WHEEL')][0]
              content = zin.read(wheel_file).decode()
          fixed = content.replace('linux_x86_64', plat)
          tmpfd, tmppath = tempfile.mkstemp(suffix='.whl')
          os.close(tmpfd)
          with zipfile.ZipFile(dst, 'r') as zin, zipfile.ZipFile(tmppath, 'w') as zout:
              for item in zin.infolist():
                  data = zin.read(item.filename)
                  if item.filename == wheel_file:
                      data = fixed.encode()
                  zout.writestr(item, data)
          shutil.move(tmppath, dst)
          print(f'Retagged: {os.path.basename(dst)}')
          PYEOF
          $CT run --rm \
            -v ${{ runner.temp }}/dist:/tmp/dist:ro \
            -v ${{ runner.temp }}/dist-pypi:/tmp/dist-pypi \
            -v ${{ runner.temp }}/retag.py:/tmp/retag.py:ro \
            $IMAGE bash -c "
              pip install auditwheel -q
              for whl in /tmp/dist/amd_mori_nightly-*.whl; do
                PLAT=\$(auditwheel show \"\$whl\" 2>&1 | grep -o 'manylinux_[0-9_]*_x86_64' | head -1)
                if [ -z \"\$PLAT\" ]; then PLAT=linux_x86_64; fi
                NEWNAME=\$(basename \"\$whl\" | sed \"s/linux_x86_64/\$PLAT/\")
                python3 /tmp/retag.py \"\$whl\" \"/tmp/dist-pypi/\$NEWNAME\" \"\$PLAT\"
              done
            "
          $CT run --rm -v ${{ runner.temp }}/dist-pypi:/tmp/dist-pypi $IMAGE \
            chown -R $(id -u):$(id -g) /tmp/dist-pypi
          echo "=== PyPI wheels ==="
          ls -lh ${{ runner.temp }}/dist-pypi/

      - name: Upload CI test wheel (amd_mori)
        uses: actions/upload-artifact@v4
        with:
          name: wheel-cp${{ matrix.python }}
          path: ${{ runner.temp }}/dist/amd_mori-*.whl
          retention-days: 30

      - name: Upload PyPI wheel
        uses: actions/upload-artifact@v4
        with:
          name: pypi-wheel-cp${{ matrix.python }}
          path: ${{ runner.temp }}/dist-pypi/amd_mori_nightly-*.whl
          retention-days: 30

      - name: Cleanup
        if: always()
        run: |
          $CT run --rm \
              -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE \
              -v ${{ runner.temp }}:/runner_temp \
              $IMAGE bash -c "
                chown -R $(id -u):$(id -g) $GITHUB_WORKSPACE 2>/dev/null
                rm -rf /runner_temp/dist
              " || true

  # ── Stage 2a: Intranode tests (install from build artifact) ────────────────
  test-wheel:
    name: intranode test (${{ matrix.platform }}, py${{ matrix.python }})
    needs: build-wheel
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: MI355X_AINIC
            python: "3.10"
            runner: [self-hosted, MI355X-AINIC-TW]
            base_image: rocm/pytorch:rocm7.2.4_ubuntu22.04_py3.10_pytorch_release_2.8.0
            rdma_devices: rdma0,rdma1,rdma2,rdma3,rdma4,rdma5,rdma6,rdma7
            rdma_sl: 3
            rdma_tc: 104

          - platform: MI355X_AINIC
            python: "3.12"
            runner: [self-hosted, MI355X-AINIC-TW]
            base_image: rocm/pytorch:rocm7.14_ubuntu24.04_py3.12_pytorch_release_2.12.0
            rdma_devices: rdma0,rdma1,rdma2,rdma3,rdma4,rdma5,rdma6,rdma7
            rdma_sl: 3
            rdma_tc: 104
          # Runner 325_bnxt_108 is decommissioned.
          # - platform: MI325X_BNXT
          #   python: "3.12"
          #   runner: [self-hosted, 325_bnxt_108]
          #   base_image: rocm/pytorch:rocm7.2.4_ubuntu24.04_py3.12_pytorch_release_2.8.0
          #   rdma_devices: bnxt_re0,bnxt_re1,bnxt_re2,bnxt_re3,bnxt_re4,bnxt_re5,bnxt_re7,bnxt_re8
          #   rdma_sl: 3
          #   rdma_tc: 104
          #   socket_ifname: enp49s0f1np1
    env:
      IMAGE: rocm/mori:ci-py${{ matrix.python }}
      MORI_RDMA_DEVICES: ${{ matrix.rdma_devices }}
      MORI_RDMA_SL: ${{ matrix.rdma_sl }}
      MORI_RDMA_TC: ${{ matrix.rdma_tc }}
      MORI_SOCKET_IFNAME: ${{ matrix.socket_ifname }}
      NCCL_SOCKET_IFNAME: ${{ matrix.socket_ifname }}
      # Perf dashboard capture (MI355X_AINIC only for now). Empty MORI_PERF_OUT
      # makes tests/python/perf_report.record_perf a no-op, so other platforms
      # are unaffected.
      MORI_PERF_OUT: ${{ matrix.platform == 'MI355X_AINIC' && format('{0}/_perf/perf.jsonl', github.workspace) || '' }}
      MORI_PERF_RUN_ID: ${{ github.run_id }}
      MORI_PERF_COMMIT: ${{ github.sha }}
      MORI_PERF_PLATFORM: ${{ matrix.platform }}
      MORI_PERF_PYTHON: ${{ matrix.python }}
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Download wheel artifact
        uses: actions/download-artifact@v4
        with:
          name: wheel-cp${{ matrix.python }}
          path: ${{ github.workspace }}/_wheels

      - name: Build CI image
        run: $CT build --network=host --build-arg BASE_IMAGE=${{ matrix.base_image }} -t $IMAGE -f docker/Dockerfile.dev .

      - name: Start container
        run: |
          $CT rm -f $CONTAINER 2>/dev/null || true
          CONTAINER_RUNTIME=$CT ./docker/ci_run.sh --name $CONTAINER \
            -e MORI_RDMA_DEVICES=$MORI_RDMA_DEVICES \
            -e MORI_RDMA_SL=$MORI_RDMA_SL \
            -e MORI_RDMA_TC=$MORI_RDMA_TC \
            ${MORI_SOCKET_IFNAME:+-e MORI_SOCKET_IFNAME=$MORI_SOCKET_IFNAME} \
            ${NCCL_SOCKET_IFNAME:+-e NCCL_SOCKET_IFNAME=$NCCL_SOCKET_IFNAME} \
            -e MORI_PERF_OUT=$MORI_PERF_OUT \
            -e MORI_PERF_RUN_ID=$MORI_PERF_RUN_ID \
            -e MORI_PERF_COMMIT=$MORI_PERF_COMMIT \
            -e MORI_PERF_PLATFORM=$MORI_PERF_PLATFORM \
            -e MORI_PERF_PYTHON=$MORI_PERF_PYTHON \
            -e MORI_PERF_DATE=$(date -u +%Y-%m-%d) \
            -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE \
            -w $GITHUB_WORKSPACE \
            $IMAGE sleep infinity
          $CT exec $CONTAINER \
            git config --global --add safe.directory $GITHUB_WORKSPACE

      - name: Install wheel (from artifact)
        run: |
          $CT exec $CONTAINER bash -c "
            pip install $GITHUB_WORKSPACE/_wheels/amd_mori-*.whl
            pip install prettytable pytest
            python3 -c 'import mori; print(\"mori \" + mori.__version__ + \" imported OK\")'
          "

      - name: MORI-EP (intranode)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE && timeout 360 pytest tests/python/ops/test_dispatch_combine_intranode.py -v
          "

      - name: MORI-EP (internode_v1)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE && timeout 300 pytest tests/python/ops/test_dispatch_combine_internode_v1.py -v
          "

      - name: MORI-EP (routing handle)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE && timeout 300 pytest tests/python/ops/test_dispatch_combine_routing_handle.py -v
          "

      - name: MORI-EP (async_ll SDMA)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE && MORI_ENABLE_SDMA=1 timeout 300 pytest tests/python/ops/test_dispatch_combine_async_ll.py -v
          "

      - name: MORI-EP (async_ll IBGDA)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE && MORI_DISABLE_P2P=1 MORI_ENABLE_SDMA=0 timeout 300 pytest tests/python/ops/test_dispatch_combine_async_ll.py -v
          "

      - name: MORI-EP bench
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE \
            -e MORI_EP_LAUNCH_CONFIG_MODE=AUTO $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            timeout 120 python3 tests/python/ops/bench_dispatch_combine.py
            timeout 120 python3 tests/python/ops/bench_dispatch_combine.py \
              --cmd bench --dtype bf16 --quant-type fp8_blockwise \
              --zero-copy 0 --max-tokens 128 \
              --force-scale-active 1 --report-scale-stats 1
          "

      - name: MORI-IO
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            timeout 60 pytest tests/python/io/ -v
            MORI_IO_XGMI_SCATTER_GATHER_THRESHOLD=4 \
              timeout 120 pytest tests/python/io/test_discrete_buffer.py -v -k 'not performance'
          "

      - name: MORI-IR
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE -e HOME=/tmp $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            timeout 60 torchrun --nproc_per_node=2 examples/shmem/ir/test_triton_shmem.py
            timeout 60 torchrun --nproc_per_node=8 examples/shmem/ir/test_triton_allreduce.py
            MORI_DISABLE_P2P=ON timeout 60 torchrun --nproc_per_node=8 examples/shmem/ir/test_triton_allreduce.py
          "

      - name: MORI-CCL/shmem
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            timeout 600 pytest tests/python/shmem/test_api.py -v
          "

      - name: MORI-CCL collectives
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            MORI_ENABLE_SDMA=1 timeout 300 python -m tests.python.ccl.test_allgather --world-size 8 --elems 1024 --iterations 1 --warmup 0
            MORI_ENABLE_SDMA=1 timeout 300 python -m tests.python.ccl.test_all2all --world-size 8 --elems 1024 --iterations 1 --warmup 0
          "

      - name: MORI-EP async kernel bench (intranode)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE \
            -e MORI_EP_LAUNCH_CONFIG_MODE=AUTO $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            PORT=12390
            for TOKENS in 64 128; do
              echo \"=== async_ll bench max-tokens=\$TOKENS port=\$PORT ===\"
              MORI_ENABLE_SDMA=1 GPU_PER_NODE=8 \
              timeout 120 torchrun \
                --nnodes=1 --node_rank=0 --nproc_per_node=1 \
                --master_addr=127.0.0.1 --master_port=\$PORT \
                examples/ops/dispatch_combine/test_dispatch_combine_internode.py \
                --kernel-type async_ll --num-qp 2 --cmd bench \
                --dtype bf16 --max-tokens \$TOKENS
              PORT=\$((PORT + 1))
            done
          "

      - name: MORI-UMBP (python)
        run: |
          $CT exec -e PYTHONPATH=$GITHUB_WORKSPACE $CONTAINER bash -c "
            cd $GITHUB_WORKSPACE
            timeout 300 pytest tests/python/umbp/ -v
          "

      - name: Fix perf data ownership
        if: always() && matrix.platform == 'MI355X_AINIC'
        run: |
          $CT run --rm -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE $IMAGE \
            chown -R $(id -u):$(id -g) $GITHUB_WORKSPACE/_perf 2>/dev/null || true
          ls -l $GITHUB_WORKSPACE/_perf 2>/dev/null || echo "no _perf dir"

      - name: Upload perf data (intra EP)
        if: always() && matrix.platform == 'MI355X_AINIC'
        uses: actions/upload-artifact@v4
        with:
          name: perf-data-intra-${{ matrix.platform }}-py${{ matrix.python }}
          path: ${{ github.workspace }}/_perf/*.jsonl
          if-no-files-found: warn
          retention-days: 45

      - name: Cleanup
        if: always()
        run: |
          $CT run --rm \
              -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE \
              $IMAGE bash -c "
                chown -R $(id -u):$(id -g) $GITHUB_WORKSPACE 2>/dev/null
              " || true
          $CT rm -f $CONTAINER || true

  # ── Stage 2b: Internode tests (install from build artifact) ────────────────
  test-wheel-internode:
    name: internode test (${{ matrix.platform }}, py${{ matrix.python }})
    needs: build-wheel
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: MI355X_AINIC
            python: "3.10"
            runner: [self-hosted, MI355X-AINIC]
            base_image: rocm/pytorch:rocm7.2.4_ubuntu22.04_py3.10_pytorch_release_2.8.0
            node1_host: 10.2.80.22
            node2_host: 10.2.80.20
            node2_port: 22
            node1_ifname: enp193s0f1np1
            node2_ifname: enp193s0f1np1
            rdma_devices: rocep105s0,rocep121s0,rocep137s0,rocep153s0,rocep233s0,rocep249s0,rocep25s0,rocep9s0
            rdma_sl: 3
            rdma_tc: 104
            ct: podman
          - platform: MI355X_AINIC
            python: "3.12"
            runner: [self-hosted, MI355X-AINIC]
            base_image: rocm/pytorch:rocm7.14_ubuntu24.04_py3.12_pytorch_release_2.12.0
            node1_host: 10.2.80.22
            node2_host: 10.2.80.20
            node2_port: 22
            node1_ifname: enp193s0f1np1
            node2_ifname: enp193s0f1np1
            rdma_devices: rocep105s0,rocep121s0,rocep137s0,rocep153s0,rocep233s0,rocep249s0,rocep25s0,rocep9s0
            rdma_sl: 3
            rdma_tc: 104
            ct: podman
          # Runner 325_bnxt_108 is decommissioned.
          # - platform: MI325X_BNXT
          #   python: "3.12"
          #   runner: [self-hosted, 325_bnxt_108]
          #   base_image: rocm/pytorch:rocm7.2.4_ubuntu24.04_py3.12_pytorch_release_2.8.0
          #   node1_host: 10.162.224.131
          #   node2_host: 10.162.224.162
          #   node2_port: 22
          #   node1_ifname: enp49s0f1np1
          #   node2_ifname: enp49s0f1np1
          #   rdma_devices: bnxt_re0,bnxt_re1,bnxt_re2,bnxt_re3,bnxt_re4,bnxt_re5,bnxt_re7,bnxt_re8
          #   rdma_sl: 3
          #   rdma_tc: 104
          #   ct: docker
    env:
      CT: ${{ matrix.ct }}
      IMAGE: rocm/mori:ci-py${{ matrix.python }}
      SSH_OPTS: -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null
      NODE1_HOST: ${{ matrix.node1_host }}
      NODE2_HOST: ${{ matrix.node2_host }}
      NODE2_PORT: ${{ matrix.node2_port }}
      NODE1_IFNAME: ${{ matrix.node1_ifname }}
      NODE2_IFNAME: ${{ matrix.node2_ifname }}
      MORI_RDMA_DEVICES: ${{ matrix.rdma_devices }}
      MORI_RDMA_SL: ${{ matrix.rdma_sl }}
      MORI_RDMA_TC: ${{ matrix.rdma_tc }}
      MORI_SOCKET_IFNAME: ${{ matrix.node1_ifname }}
      # Perf dashboard capture on node1/rank0 (MI355X_AINIC only for now).
      MORI_PERF_OUT: ${{ matrix.platform == 'MI355X_AINIC' && format('{0}/_perf/perf.jsonl', github.workspace) || '' }}
      MORI_PERF_RUN_ID: ${{ github.run_id }}
      MORI_PERF_COMMIT: ${{ github.sha }}
      MORI_PERF_PLATFORM: ${{ matrix.platform }}
      MORI_PERF_PYTHON: ${{ matrix.python }}
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Download wheel artifact
        uses: actions/download-artifact@v4
        with:
          name: wheel-cp${{ matrix.python }}
          path: ${{ github.workspace }}/_wheels

      - name: Build CI image
        run: $CT build --network=host --build-arg BASE_IMAGE=${{ matrix.base_image }} -t $IMAGE -f docker/Dockerfile.dev .

      - name: Start container on node1
        run: |
          $CT rm -f $CONTAINER 2>/dev/null || true
          CONTAINER_RUNTIME=$CT ./docker/ci_run.sh --name $CONTAINER \
            -e MORI_RDMA_DEVICES=$MORI_RDMA_DEVICES \
            -e MORI_RDMA_SL=$MORI_RDMA_SL \
            -e MORI_RDMA_TC=$MORI_RDMA_TC \
            -e MORI_SOCKET_IFNAME=$MORI_SOCKET_IFNAME \
            -e GLOO_SOCKET_IFNAME=$NODE1_IFNAME \
            -e NCCL_SOCKET_IFNAME=$NODE1_IFNAME \
            -e MORI_PERF_OUT=$MORI_PERF_OUT \
            -e MORI_PERF_RUN_ID=$MORI_PERF_RUN_ID \
            -e MORI_PERF_COMMIT=$MORI_PERF_COMMIT \
            -e MORI_PERF_PLATFORM=$MORI_PERF_PLATFORM \
            -e MORI_PERF_PYTHON=$MORI_PERF_PYTHON \
            -e MORI_PERF_DATE=$(date -u +%Y-%m-%d) \
            -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE \
            -w $GITHUB_WORKSPACE \
            $IMAGE sleep infinity
          $CT exec $CONTAINER \
            git config --global --add safe.directory $GITHUB_WORKSPACE

      - name: Start container on node2
        run: |
          ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "
            $CT rm -f $CONTAINER 2>/dev/null || true
            mkdir -p $GITHUB_WORKSPACE
          "
          rsync -az --exclude='.git' \
            -e "ssh $SSH_OPTS -p $NODE2_PORT" \
            $GITHUB_WORKSPACE/ \
            $(whoami)@$NODE2_HOST:$GITHUB_WORKSPACE/
          ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "
            cd $GITHUB_WORKSPACE &&
            $CT build --network=host --build-arg BASE_IMAGE=${{ matrix.base_image }} -t $IMAGE -f docker/Dockerfile.dev . &&
            CONTAINER_RUNTIME=$CT ./docker/ci_run.sh --name $CONTAINER \
              -e MORI_RDMA_DEVICES=$MORI_RDMA_DEVICES \
              -e MORI_RDMA_SL=$MORI_RDMA_SL \
              -e MORI_RDMA_TC=$MORI_RDMA_TC \
              -e MORI_SOCKET_IFNAME=$MORI_SOCKET_IFNAME \
              -e GLOO_SOCKET_IFNAME=$NODE2_IFNAME \
              -e NCCL_SOCKET_IFNAME=$NODE2_IFNAME \
              -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE \
              -w $GITHUB_WORKSPACE \
              $IMAGE sleep infinity &&
            $CT exec $CONTAINER \
              git config --global --add safe.directory $GITHUB_WORKSPACE
          "

      - name: Install wheel (node1)
        run: |
          $CT exec $CONTAINER bash -c "
            pip install $GITHUB_WORKSPACE/_wheels/amd_mori-*.whl
            pip install prettytable
          "

      - name: Install wheel (node2)
        run: |
          ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST \
            "$CT exec $CONTAINER bash -c '
              pip install $GITHUB_WORKSPACE/_wheels/amd_mori-*.whl
              pip install prettytable
            '"

      - name: MORI-IO internode write sweep
        run: |
          SCRIPT="$GITHUB_WORKSPACE/tools/run_internode_io_benchmark.sh"
          PORT=29120
          echo "=== MORI-IO internode benchmark: wide-write-sweep port=$PORT ==="
          NODE2_CMD=(
            $CT exec $CONTAINER bash $SCRIPT
            --rank 1 --master-addr $NODE1_HOST --master-port $PORT
            --ifname $NODE2_IFNAME
            -- --op-type write --buffer-size 65536 --transfer-batch-size 256 --all
            --sweep-start-size 1024 --sweep-max-size 16777216 --iters 128
            --batch-contiguous --enable-sess --enable-batch-transfer
            --num-qp-per-transfer 8 --num-worker-threads 8
            --num-initiator-dev 8 --num-target-dev 8
          )
          ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "${NODE2_CMD[*]}" &
          $CT exec $CONTAINER bash $SCRIPT \
            --rank 0 --master-addr $NODE1_HOST --master-port $PORT \
            --ifname $NODE1_IFNAME \
            -- --op-type write --buffer-size 65536 --transfer-batch-size 256 --all \
            --sweep-start-size 1024 --sweep-max-size 16777216 --iters 128 \
            --batch-contiguous --enable-sess --enable-batch-transfer \
            --num-qp-per-transfer 8 --num-worker-threads 8 \
            --num-initiator-dev 8 --num-target-dev 8
          wait

      # Correctness/smoke check only: a fixed 4KB single-point read, not a sweep.
      # The numbers are far below what the write sweep reports, so this step runs
      # with MORI_PERF_OUT cleared and contributes nothing to the perf report.
      # Config intentionally matches ci.yml (2 QP, no --batch-contiguous, no
      # --disable-chunking): merging the batch into one WR starves the
      # `--poll_cq_mode event` CQ loop and the step hangs until the 600s timeout.
      - name: MORI-IO internode read
        run: |
          SCRIPT="$GITHUB_WORKSPACE/tools/run_internode_io_benchmark.sh"
          PORT=29121
          echo "=== MORI-IO internode benchmark: batch-session-read port=$PORT ==="
          NODE2_CMD=(
            $CT exec -e MORI_PERF_OUT= $CONTAINER bash $SCRIPT
            --rank 1 --master-addr $NODE1_HOST --master-port $PORT
            --ifname $NODE2_IFNAME
            -- --op-type read --buffer-size 4096 --transfer-batch-size 128 --iters 8
            --enable-batch-transfer --enable-sess --poll_cq_mode event
            --num-qp-per-transfer 2 --num-worker-threads 2
            --num-initiator-dev 8 --num-target-dev 8
          )
          ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "${NODE2_CMD[*]}" &
          $CT exec -e MORI_PERF_OUT= $CONTAINER bash $SCRIPT \
            --rank 0 --master-addr $NODE1_HOST --master-port $PORT \
            --ifname $NODE1_IFNAME \
            -- --op-type read --buffer-size 4096 --transfer-batch-size 128 --iters 8 \
            --enable-batch-transfer --enable-sess --poll_cq_mode event \
            --num-qp-per-transfer 2 --num-worker-threads 2 \
            --num-initiator-dev 8 --num-target-dev 8
          wait

      - name: Build MORI-IO C++ bench_engine (both nodes)
        run: |
          BUILD_CMD="cd $GITHUB_WORKSPACE && cmake -B build -DBUILD_IO=ON -DBUILD_TESTS=ON -DBUILD_EXAMPLES=OFF -DBUILD_PYBINDS=OFF&& cmake --build build --target bench_engine -j && ls -la build/tests/cpp/bench_engine"
          $CT exec $CONTAINER bash -c "$BUILD_CMD"
          ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "$CT exec $CONTAINER bash -c \"$BUILD_CMD\""

      - name: MORI-IO C++ bench_engine write sweep (batch 1 and 64)
        run: |
          BIN=$GITHUB_WORKSPACE/build/tests/cpp/bench_engine
          LDP="LD_LIBRARY_PATH=$GITHUB_WORKSPACE/build/src/io:$GITHUB_WORKSPACE/build/src/application"
          PORT=29130
          for BATCH in 1 64; do
            echo "=== C++ bench_engine GPU write sweep, batch=$BATCH, port=$PORT ==="
            ARGS="--op write --mem-type gpu --all --sweep-start 1048576 --sweep-max 33554432 --sweep-step 1048576 --transfer-batch-size $BATCH --enable-sess --enable-batch-transfer --num-initiator-dev 1 --num-target-dev 1 --num-qp-per-transfer 8 --iters 200 --warmup-iters 5"
            R1="cd $GITHUB_WORKSPACE && $LDP $BIN --rank 1 --master-ip $NODE1_HOST --self-ip $NODE2_HOST --port $PORT $ARGS"
            R0="cd $GITHUB_WORKSPACE && $LDP $BIN --rank 0 --master-ip $NODE1_HOST --self-ip $NODE1_HOST --port $PORT $ARGS"
            ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "$CT exec $CONTAINER bash -c \"$R1\"" &
            $CT exec $CONTAINER bash -c "$R0"
            wait
            PORT=$((PORT + 1))
          done

      - name: MORI-EP internode normal kernel bench
        run: |
          SCRIPT="$GITHUB_WORKSPACE/tools/run_internode_test.sh"
          PORT=29140
          # Launch params auto-selected from tuning_configs via AUTO mode
          # (bf16/fp8/fp4 rules per num_tokens/hidden_dim, dispatch & combine).
          for KERNEL in v1 v1_ll; do
            for TOKENS in 64 128 1024 2048 4096; do
              echo "=== bench kernel=$KERNEL max-tokens=$TOKENS port=$PORT ==="
              NODE2_CMD=(
                $CT exec -e MORI_INTERNODE_TIMEOUT=600 -e MORI_EP_LAUNCH_CONFIG_MODE=AUTO $CONTAINER bash $SCRIPT
                --rank 1 --master-addr $NODE1_HOST --master-port $PORT
                --ifname $NODE2_IFNAME
                --cmd bench --kernel-type $KERNEL --max-tokens $TOKENS
              )
              ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "${NODE2_CMD[*]}" &
              $CT exec -e MORI_INTERNODE_TIMEOUT=600 -e MORI_EP_LAUNCH_CONFIG_MODE=AUTO $CONTAINER bash $SCRIPT \
                --rank 0 --master-addr $NODE1_HOST --master-port $PORT \
                --ifname $NODE1_IFNAME \
                --cmd bench --kernel-type $KERNEL --max-tokens $TOKENS
              wait
              sleep 1
              PORT=$((PORT + 1))
            done
          done

      - name: MORI-EP internode normal kernel stress
        run: |
          SCRIPT="$GITHUB_WORKSPACE/tools/run_internode_test.sh"
          PORT=29160
          for KERNEL in v1 v1_ll; do
            for TOKENS in 64 128 1024 2048 4096; do
              echo "=== stress kernel=$KERNEL max-tokens=$TOKENS port=$PORT ==="
              NODE2_CMD=(
                $CT exec -e MORI_INTERNODE_TIMEOUT=600 $CONTAINER bash $SCRIPT
                --rank 1 --master-addr $NODE1_HOST --master-port $PORT
                --ifname $NODE2_IFNAME
                --cmd stress --kernel-type $KERNEL --max-tokens $TOKENS
              )
              ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "${NODE2_CMD[*]}" &
              $CT exec -e MORI_INTERNODE_TIMEOUT=600 $CONTAINER bash $SCRIPT \
                --rank 0 --master-addr $NODE1_HOST --master-port $PORT \
                --ifname $NODE1_IFNAME \
                --cmd stress --kernel-type $KERNEL --max-tokens $TOKENS
              wait
              sleep 1
              PORT=$((PORT + 1))
            done
          done

      - name: MORI-EP internode async kernel test
        run: |
          SCRIPT="$GITHUB_WORKSPACE/tools/run_internode_test.sh"
          PORT=29180
          for TOKENS in 64 128; do
            echo "=== async_ll test max-tokens=$TOKENS port=$PORT ==="
            NODE2_CMD=(
              $CT exec
              -e GPU_PER_NODE=8 -e MORI_ENABLE_SDMA=1 -e MORI_INTERNODE_TIMEOUT=600
              $CONTAINER bash $SCRIPT
              --rank 1 --master-addr $NODE1_HOST --master-port $PORT
              --ifname $NODE2_IFNAME
              --cmd test --kernel-type async_ll
              --quant-type none --dtype bf16 --max-tokens $TOKENS
            )
            ssh $SSH_OPTS -p $NODE2_PORT $(whoami)@$NODE2_HOST "${NODE2_CMD[*]}" &
            $CT exec \
              -e GPU_PER_NODE=8 \
              -e MORI_ENABLE_SDMA=1 \
              -e MORI_INTERNODE_TIMEOUT=600 \
              $CONTAINER bash $SCRIPT \
              --rank 0 --master-addr $NODE1_HOST --master-port $PORT \
              --ifname $NODE1_IFNAME \
              --cmd test --kernel-type async_ll \
              --quant-type none --dtype bf16 --max-tokens $TOKENS
            wait
            sleep 1
            PORT=$((PORT + 1))
          done

      - name: Fix perf data ownership (node1)
        if: always() && matrix.platform == 'MI355X_AINIC'
        run: |
          if $CT image inspect $IMAGE &>/dev/null; then
            $CT run --rm -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE $IMAGE \
              chown -R $(id -u):$(id -g) $GITHUB_WORKSPACE/_perf 2>/dev/null || true
          fi
          ls -l $GITHUB_WORKSPACE/_perf 2>/dev/null || echo "no _perf dir"

      - name: Upload perf data (internode EP + IO)
        if: always() && matrix.platform == 'MI355X_AINIC'
        uses: actions/upload-artifact@v4
        with:
          name: perf-data-internode-${{ matrix.platform }}-py${{ matrix.python }}
          path: ${{ github.workspace }}/_perf/*.jsonl
          if-no-files-found: warn
          retention-days: 45

      - name: Cleanup node1
        if: always()
        run: |
          $CT rm -f $CONTAINER || true
          if $CT image inspect $IMAGE &>/dev/null; then
            $CT run --rm -v $GITHUB_WORKSPACE:$GITHUB_WORKSPACE $IMAGE \
                chown -R $(id -u):$(id -g) $GITHUB_WORKSPACE 2>/dev/null || true
          fi

      - name: Cleanup node2
        if: always()
        run: |
          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
            -p $NODE2_PORT $(whoami)@$NODE2_HOST \
            "$CT rm -f $CONTAINER || true"

  # ── Stage 2c: Performance report ───────────────────────────────────────────
  # Aggregates the perf JSON emitted by the intra/internode/IO benchmarks into a
  # Markdown report rendered directly in the run's job summary. Everything lives
  # in build artifacts (no gh-pages / git commits): the current run's perf-data-*
  # artifacts plus a best-effort pull of recent nightly runs' artifacts so the
  # report can show a short per-metric trend across runs.
  perf-report:
    name: perf report
    needs: [test-wheel, test-wheel-internode]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Download this run's perf data
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          pattern: perf-data-*
          # No merge-multiple: every artifact contains a file named perf.jsonl,
          # so merging into one dir would overwrite them. Keep each artifact in
          # its own subdir; build_report.py globs *.jsonl recursively.
          path: perf_in/current

      - name: Fetch history from recent nightly runs (best-effort)
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p perf_in/history
          ids=$(gh run list --workflow nightly.yml \
                  --branch "${{ github.ref_name }}" \
                  --status completed --limit 15 \
                  --json databaseId -q '.[].databaseId' || true)
          for id in $ids; do
            [ "$id" = "${{ github.run_id }}" ] && continue
            echo "Fetching perf artifacts from run $id"
            gh run download "$id" --pattern 'perf-data-*' \
              --dir "perf_in/history/$id" || true
          done
          echo "History fetched:"; find perf_in/history -name '*.jsonl' 2>/dev/null || true

      - name: Build report
        run: |
          python3 tools/perf/build_report.py \
            --input-dir perf_in \
            --output-dir perf_report
          cat perf_report/report.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload perf report
        uses: actions/upload-artifact@v4
        with:
          name: perf-report
          path: perf_report
          retention-days: 45

  # ── Stage 3: Test PyPI wheel (install + import verification) ────────────────
  test-pypi-wheel:
    name: test pypi wheel (py${{ matrix.python }})
    needs: [test-wheel, test-wheel-internode]
    if: github.event_name != 'pull_request'
    runs-on: [self-hosted, MI355X-AINIC-TW]
    strategy:
      fail-fast: false
      matrix:
        include:
          - python: "3.10"
            base_image: rocm/pytorch:rocm7.2.4_ubuntu22.04_py3.10_pytorch_release_2.8.0
          - python: "3.12"
            base_image: rocm/pytorch:rocm7.14_ubuntu24.04_py3.12_pytorch_release_2.12.0
    env:
      IMAGE: rocm/mori:ci-py${{ matrix.python }}
    steps:
      - name: Download PyPI wheel
        uses: actions/download-artifact@v4
        with:
          name: pypi-wheel-cp${{ matrix.python }}
          path: ${{ runner.temp }}/pypi-wheel

      - name: Build CI image
        run: $CT build --network=host --build-arg BASE_IMAGE=${{ matrix.base_image }} -t $IMAGE -f docker/Dockerfile.dev .

      - name: Test PyPI wheel
        run: |
          $CT run --rm --network=host \
            --device=/dev/kfd --device=/dev/dri \
            -v ${{ runner.temp }}/pypi-wheel:/tmp/wheel:ro \
            $IMAGE bash -c "
              pip install /tmp/wheel/*.whl
              python3 -c 'import mori; print(\"amd-mori-nightly \" + mori.__version__ + \" OK\")'
              python3 -c 'from mori import ops; print(\"mori.ops OK\")'
            "

  # ── Stage 4: Publish to PyPI ───────────────────────────────────────────────
  publish-pypi:
    name: publish to PyPI
    needs: [test-pypi-wheel]
    # Only publish from main; test runs on other branches build+test but never upload.
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Download PyPI wheels
        uses: actions/download-artifact@v4
        with:
          pattern: pypi-wheel-*
          merge-multiple: true
          path: ./dist

      - name: List wheels
        run: ls -lh ./dist/

      - name: Upload to PyPI
        run: |
          pip install twine "packaging>=24.2"
          python3 -m twine upload --skip-existing ./dist/*.whl
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
